# Enable OpenMP only when available for the active toolchain.
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_CXX17FLAGS = $(SHLIB_OPENMP_CXXFLAGS)

# Link OpenMP only when it is available on the active toolchain.
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)
